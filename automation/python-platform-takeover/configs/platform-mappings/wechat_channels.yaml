selectors:
  publish_frame: "iframe[name='content']"
  management_frame: "iframe[name='content']"
  video_file_input: "input[type='file']"
  description_input_candidates:
    - ".input-editor"
    - "textarea"
  short_title_candidates:
    - "input[placeholder='概括视频主要内容，字数建议6-16个字符']"
    - "input[placeholder*='6-16']"
    - "input"
  cover_file_inputs:
    - "input[type='file'][accept*='image']"
    - "input[accept*='image']"
  cover_file_input_index: 0
  management_entry_candidates:
    - "tr"
    - "li"
    - "article"
    - "[role='row']"
    - "div[data-id]"
    - "div[class*='post']"
    - "div[class*='item']"
buttons:
  cover_entry:
    - "编辑"
    - "编辑封面"
  cover_confirm:
    - "确认"
    - "确定"
  publish:
    - "发表"
signals:
  login_required:
    - "扫码登录"
    - "请使用微信扫码登录"
  pending_upload:
    - "请上传视频"
    - "取消上传"
    - "0%"
    - "文件上传中，请等待完成后再发表。"
  success:
    - "已发表"
  cover_applied:
    - "封面已更新"
    - "封面检测中"
    - "更换封面"
  cover_pending:
    - "上传图片"
    - "选择封面"
    - "封面上传中"
checkpoints:
  manual:
    - "登录失效"
    - "上传卡住"
    - "封面状态无法自动确认"
stops:
  duplicate_conditions:
    - "视频管理已有同视频"
    - "当前 visible UI 和 frame 真实状态不一致"
verify:
  management_status:
    - "已发表"
    - "审核中"
    - "处理中"
  max_management_candidates: 80
