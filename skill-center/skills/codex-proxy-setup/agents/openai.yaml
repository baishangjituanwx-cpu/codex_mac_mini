interface:
  display_name: "Codex Proxy Setup"
  short_description: "Detect and configure Codex HTTP/HTTPS proxy"
  default_prompt: "Use $codex-proxy-setup to diagnose and configure the local Codex HTTP/HTTPS proxy."
